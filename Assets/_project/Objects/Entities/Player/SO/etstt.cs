using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class etstt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        var pis = new Transform[] { };
        Debug.Log(pis.ToArray().Length);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
